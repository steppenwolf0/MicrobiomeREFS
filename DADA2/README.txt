The sequence cutting parameters for the quality process (filtering operation) are as follows:

Discovery dataset David et al.: trimLeft = 10,
Validation dataset PRJNA578223: truncLen = c(290,220),
Validation dataset PRJNA589343: truncLen=c(250),

These values are also found at the beginning of the script between comments.